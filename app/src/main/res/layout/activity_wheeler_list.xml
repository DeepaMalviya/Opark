<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="attender.oparkReceiptNew.vehiclelist.activity.WheelerList"
    tools:showIn="@layout/activity_wheeler_list">


    <include layout="@layout/toolbar" />

    <RadioGroup
        android:id="@+id/radiogroupAAAA"
        android:layout_width="wrap_content"

        android:layout_height="wrap_content"
        android:layout_below="@+id/toolBar"
        android:layout_gravity="center"
        android:layout_marginTop="@dimen/_10sdp"
        android:layout_marginBottom="@dimen/_10sdp"
        android:gravity="center"
        android:orientation="vertical" />

    <android.support.design.widget.TabLayout
        android:id="@+id/tabLayout"
        style="@style/MyCustomTabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/radiogroupAAAA"
        app:tabBackground="@android:color/holo_blue_light"
        app:tabIndicatorColor="@color/red"
        app:tabSelectedTextColor="@color/white"
        app:tabTextColor="@color/white" />

    <android.support.v4.view.ViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tabLayout"
        android:layout_marginLeft="4dp"
        android:layout_marginRight="4dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

</RelativeLayout>
